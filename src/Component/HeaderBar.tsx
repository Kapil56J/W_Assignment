import React from 'react';
import { Typography } from "@mui/material";

const HeaderBar = () => {

    return (
        <div>
            <Typography variant="h4">Hello React JS</Typography>
        </div>
    );
};

export default HeaderBar;
